import React from "react";

export default function TextArea({}) {

    return (
        <div className="max-w-xl max-h-fit hero container mx-auto py-20 pb-10 justify-center">
            <h1 className="font-lobster text-7xl"> Aimie's Art Gallery </h1>
            <p className="font-serif py-10 text-2xl"> "If I could say it in words there would be no reason to paint." - Edward Hopper</p>
        </div>
    );
}